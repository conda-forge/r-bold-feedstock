{% set version = "0.9.0" %}

package:
  name: r-bold
  version: {{ version }}

source:
  fn: bold_{{ version }}.tar.gz
  url:
    - https://cran.r-project.org/src/contrib/bold_{{ version }}.tar.gz
    - https://cran.r-project.org/src/contrib/Archive/bold/bold_{{ version }}.tar.gz
  sha256: 45e844a83f4545a2f84887e36db83113da824a8673fa039f067a3bd7ee82ed5e

build:
  noarch: generic
  number: 0
  script: R CMD INSTALL --build .
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - r-base
    - r-crul >=0.3.8
    - r-data.table
    - r-jsonlite
    - r-plyr
    - r-reshape
    - r-stringr
    - r-tibble
    - r-xml2
  run:
    - r-base
    - r-crul >=0.3.8
    - r-data.table
    - r-jsonlite
    - r-plyr
    - r-reshape
    - r-stringr
    - r-tibble
    - r-xml2

test:
  commands:
    - $R -e "library('bold')"  # [not win]
    - "\"%R%\" -e \"library('bold')\""  # [win]

about:
  home: https://cran.r-project.org/web/packages/bold
  license: MIT
  license_file: LICENSE
  summary: A programmatic interface to the Web Service methods provided by Bold Systems (<http

extra:
  recipe-maintainers:
    - dpryan79
    - ocefpaf
